version: '3.9'

services:
    frontend:
        image: node:20-alpine
        working_dir: /app
        volumes:
            - .:/app
        ports:
            - "5173:5173"
        command: sh -c "npm install && npm run dev -- --host"
