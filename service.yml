apiVersion: v1
kind: Service
metadata:
  namespace: vcare
  name: vcare-frontend
  labels:
    app: vcare-frontend
spec:
  selector:
    app: vcare-frontend
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 80
  type: NodePort
